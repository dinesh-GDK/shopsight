name: shopsight-backend
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
      # Core Framework
      - fastapi==0.104.1
      - uvicorn[standard]==0.24.0
      - pydantic==2.5.0
      - pydantic-settings==2.1.0

      # Database
      - duckdb==0.9.2

      # LLM Integration
      - ollama==0.1.6

      # Data Processing
      - pandas==2.1.3
      - numpy==1.26.2

      # Utilities
      - python-dotenv==1.0.0
      - python-multipart==0.0.6

      # Testing
      - pytest==7.4.3
      - pytest-asyncio==0.21.1
      - httpx==0.25.2

      # Development
      - black==23.11.0
      - ruff==0.1.6
